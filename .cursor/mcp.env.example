# Cursor MCP credentials (copy to .cursor/mcp.env — gitignored)
# App keys (Clerk, Supabase, Didit) belong in .env, not here.

# GitHub MCP (optional) — fine-grained PAT, repo scope: natew-dev/vera
GITHUB_PERSONAL_ACCESS_TOKEN=

# Add other MCP plugin tokens only if you use those servers
